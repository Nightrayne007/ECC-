export default function AudioPlayerStub() {
  return (
    <div className="rounded border px-3 py-2 text-sm" style={{ borderColor: "var(--gridline)", color: "var(--text-muted)" }}>
      Audio playback not available in Phase 1 mock mode.
    </div>
  );
}
